import 'package:flutter/material.dart';

class SortTeamsPage extends StatelessWidget {
  const SortTeamsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Sortear Times'),
    );
  }
}
